{
  "version": "2",
  "scripts": {
    "dev": "concurrently -k \"cd api && ts-node index.ts\" \"cd client && npm start\" ",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@types/styled-components": "^5.1.25",
    "@zach.codes/react-calendar": "^0.3.1",
    "concurrently": "^7.1.0"
  }
}
